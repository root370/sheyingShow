# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Tencent Cloud COS Configuration
# 访问控制 -> API密钥管理 获取
TENCENT_SECRET_ID=your_secret_id
TENCENT_SECRET_KEY=your_secret_key

# 对象存储 -> 存储桶列表 获取
# 格式: bucketname-appid (例如: exhibition-1250000000)
NEXT_PUBLIC_TENCENT_BUCKET=your_bucket_name
# 格式: ap-shanghai, ap-beijing 等
NEXT_PUBLIC_TENCENT_REGION=ap-shanghai

# CDN Configuration (Optional but Recommended)
# 如果配置了 CDN，请填写 CDN 加速域名 (例如: https://static.yourdomain.com)
# 如果没有 CDN，留空即可，将使用 COS 默认域名
NEXT_PUBLIC_CDN_HOST=https://your-cdn-domain.com
